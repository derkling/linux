#
# Testing CPM support
#

menuconfig CPM_TESTING
	bool "Enable Testing support"
	default n
	---help--- 
	  Say Y here to enable CPM support.
	  
if CPM_TESTING

config CPM_BUDDY
	tristate "buddy test driver"
	depends on EXPERIMENTAL
	help
	  If you say yes todo.

endif # CPM_TESTING

